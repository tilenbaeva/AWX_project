---
  - name: unziping wordpress 
    hosts: all
    become: yes
    become_method: sudo
    tasks:
      - name: download the link
      command: "wget https://wordpress.org/latest.tar.gz"
        name: unzip the file
      command: "gunzip wordpress-5.1.1.tar.gz /tmp)"
